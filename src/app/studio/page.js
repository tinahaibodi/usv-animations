import FlywheelStudio from "@/components/flywheel/flywheel-scene";

export const metadata = {
  title: "Animation Studio | USV",
  description: "Scrub and export flywheel animation frames for editorial use.",
};

export default function StudioPage() {
  return <FlywheelStudio />;
}
